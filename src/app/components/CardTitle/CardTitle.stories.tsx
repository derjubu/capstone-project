import React from 'react';
import CardTitle from './CardTitle';

export default {
  title: 'Component/CardTitle',
  component: CardTitle,
};

export const London = (): JSX.Element => (
  <CardTitle>London, Greater London, England, United Kingdom</CardTitle>
);
